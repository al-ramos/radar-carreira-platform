"use client";
import { useEffect,useState } from "react";
type Source={id:string;name:string;provider:string;collectionMode:"pull"|"push";externalRef:string|null;enabled:boolean;lastRunAt:string|null;canCollect:boolean};
export default function SourceList(){const[sources,setSources]=useState<Source[]>([]),[status,setStatus]=useState("Carregando fontes…");useEffect(()=>{fetch("/api/admin/sources").then(async r=>({ok:r.ok,data:await r.json()})).then(({ok,data})=>{if(ok){setSources(data.sources);setStatus("")}else setStatus("Não foi possível carregar as fontes.")})},[]);
 async function toggle(source:Source){const r=await fetch("/api/admin/sources",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id:source.id,enabled:!source.enabled})});if(r.ok){setSources(list=>list.map(item=>item.id===source.id?{...item,enabled:!item.enabled}:item));setStatus(!source.enabled?"Fonte reativada.":"Fonte pausada.")}}
 const date=(v:string|null)=>v?new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(new Date(v)):"Ainda não coletada";
 const automatic=sources.filter(source=>source.collectionMode==="pull"),integrations=sources.filter(source=>source.collectionMode==="push");
 const card=(source:Source)=> <article key={source.id}><i className={source.enabled?"on":"off"}/><span><b>{source.name}</b><small>{source.collectionMode==="pull"?`${source.provider} · ${source.externalRef}`:"Integração de envio"}<br/>{source.collectionMode==="pull"?`Última coleta: ${date(source.lastRunAt)}`:"Recebe vagas enviadas por outro serviço"}</small></span><button onClick={()=>toggle(source)}>{source.enabled?"Pausar":"Reativar"}</button></article>;
 return <section className="source-manager">{status&&<div className="notice">{status}</div>}<section className="source-group"><h3>Coleta automática <span>{automatic.filter(source=>source.enabled).length} ativas</span></h3><p>O Radar consulta estas páginas de carreira por conta própria.</p>{automatic.map(card)}{!status&&automatic.length===0&&<div className="source-empty">Nenhuma fonte automática cadastrada. Adicione uma empresa Greenhouse, Lever ou Ashby acima.</div>}</section><section className="source-group"><h3>Integrações de entrada <span>{integrations.filter(source=>source.enabled).length} conectadas</span></h3><p>Estas integrações enviam vagas ao Radar; elas não participam da coleta automática.</p>{integrations.map(card)}{!status&&integrations.length===0&&<div className="source-empty">Nenhuma integração configurada.</div>}</section></section>}
