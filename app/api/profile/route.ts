import { eq } from "drizzle-orm";import { NextResponse } from "next/server";import { getChatGPTUser } from "../../chatgpt-auth";import { getDb } from "../../../db/index";import { profiles } from "../../../db/schema";
const ADMINS=new Set(["contato@amrsolution.com.br"]);export const dynamic="force-dynamic";
export async function GET(){const user=await getChatGPTUser();if(!user)return NextResponse.json({user:null},{status:401});const db=getDb();let profile=(await db.select().from(profiles).where(eq(profiles.userId,user.userId)).limit(1))[0];if(!profile){profile={userId:user.userId,email:user.email,name:user.fullName,role:ADMINS.has(user.email.toLowerCase())?"admin":"user",seniority:null,preferredMode:null,cities:"[]",masteredSkills:"[]",desiredAreas:"[]",avoidTerms:"[]",minScore:60,updatedAt:new Date()};await db.insert(profiles).values(profile)}return NextResponse.json({user:{...user,role:profile.role},profile:{...profile,cities:JSON.parse(profile.cities),masteredSkills:JSON.parse(profile.masteredSkills),desiredAreas:JSON.parse(profile.desiredAreas),avoidTerms:JSON.parse(profile.avoidTerms)}})}
export async function PUT(request:Request){const user=await getChatGPTUser();if(!user)return NextResponse.json({error:"Autenticação necessária"},{status:401});const b=await request.json() as Record<string,unknown>,arr=(k:string)=>JSON.stringify(Array.isArray(b[k])?b[k]:[]),values={userId:user.userId,email:user.email,name:user.fullName,role:ADMINS.has(user.email.toLowerCase())?"admin" as const:"user" as const,seniority:String(b.seniority??""),preferredMode:String(b.preferredMode??""),cities:arr("cities"),masteredSkills:arr("masteredSkills"),desiredAreas:arr("desiredAreas"),avoidTerms:arr("avoidTerms"),minScore:Number(b.minScore??60),updatedAt:new Date()};await getDb().insert(profiles).values(values).onConflictDoUpdate({target:profiles.userId,set:values});return NextResponse.json({ok:true})}
