import { eq } from "drizzle-orm";import { NextResponse } from "next/server";import { getChatGPTUser } from "../../../chatgpt-auth";import { getDb } from "../../../../db/index";import { importRuns,jobs,profiles } from "../../../../db/schema";import { fingerprint,validJob,type ImportedJob } from "../../../../lib/jobs";
export const dynamic="force-dynamic";
export async function POST(request:Request){const user=await getChatGPTUser();if(!user)return NextResponse.json({error:"Autenticação necessária"},{status:401});const profile=(await getDb().select({role:profiles.role}).from(profiles).where(eq(profiles.userId,user.userId)).limit(1))[0];const admin=profile?.role==="admin"||user.email.toLowerCase()==="contato@amrsolution.com.br";if(!admin)return NextResponse.json({error:"Acesso de administrador necessário"},{status:403});const payload=await request.json();const items=(Array.isArray(payload)?payload:payload.jobs) as unknown[];if(!Array.isArray(items))return NextResponse.json({error:"Envie uma lista de vagas ou { jobs: [...] }"},{status:400});const runId=crypto.randomUUID(),db=getDb(),startedAt=new Date();await db.insert(importRuns).values({id:runId,source:"manual",status:"running",received:items.length,actorUserId:user.userId,startedAt});let inserted=0,updated=0,errors=0;for(const item of items){if(!validJob(item)){errors++;continue}const job=item as ImportedJob,fp=fingerprint(job),now=new Date(),existing=(await db.select({id:jobs.id}).from(jobs).where(eq(jobs.fingerprint,fp)).limit(1))[0];const values={id:existing?.id??crypto.randomUUID(),fingerprint:fp,sourceId:job.sourceId??null,externalId:job.externalId??null,company:job.company.trim(),title:job.title.trim(),seniority:job.seniority??null,workMode:job.workMode??null,location:job.location??null,stack:JSON.stringify(job.stack??[]),publishedAt:job.publishedAt?new Date(job.publishedAt):null,url:job.url,description:job.description??"",firstSeenAt:now,lastSeenAt:now,status:"active" as const,createdAt:now,updatedAt:now};await db.insert(jobs).values(values).onConflictDoUpdate({target:jobs.fingerprint,set:{company:values.company,title:values.title,seniority:values.seniority,workMode:values.workMode,location:values.location,stack:values.stack,publishedAt:values.publishedAt,url:values.url,description:values.description,lastSeenAt:now,status:"active",updatedAt:now}});existing?updated++:inserted++}await db.update(importRuns).set({status:"completed",inserted,updated,errors,duplicates:0,finishedAt:new Date()}).where(eq(importRuns.id,runId));return NextResponse.json({ok:true,runId,received:items.length,inserted,updated,errors})}
